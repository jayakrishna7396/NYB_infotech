// Ternary Operator
// Definition:
// Ternary operator is a short form of if...else.

// Syntax:
// condition ? valueIfTrue : valueIfFalse;

let age = 20;

let result = age >= 18 ? "Adult" : "Minor";

console.log(result);



//example
let marks = 45;
let score = marks >= 40 ? "Pass" : "Fail";

console.log(score);

let hello="hello"
if (hello){

console.log("truthy value")
}